# Number data types store numeric values. The object is created when the available is initialized. 
# The number data type suitable for these steps is determined and created.

number_1 = 10
number_2 = 20
number_3 = 30

# Because it is an object, it can be deleted from memory at runtime. 
# For this operation, the "del" statement is used. For example,

del number_1, number_2,number_3

#etc. For more efficient use of memory, you can delete your unnecessary variables with the del command.

test = 100
print(test)
del test
print(test) # It will be give an error.